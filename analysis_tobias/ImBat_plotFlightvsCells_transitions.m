function [flightTransitions] = ImBat_plotFlightvsCells_transitions(snakeTrace,flightTransitions)
%plot the pre/post/and flight traces for each cell by their transitions

for cell_i = 1:length(snakeTrace.

end