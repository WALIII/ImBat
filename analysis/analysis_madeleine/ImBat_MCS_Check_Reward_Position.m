function [reward_positions] = ImBat_MCS_Check_Reward_Position()

% plot a scatter  the XY cord of
    %The reward (for the double rewards or all)
    %The position of the bat when the reward happens

%This would be to check if the reward is being triggered 
%when the bat is not at the feeder




end