function ImBat_AcrossDays(AcrossData);


%% Load all Data from source if no input matrix

% Enter each folder

% Is there a MaxProj? If so, load it. If not, run it

% Load flight/alignment data

% Load trace data

% Save grouped data AcrossData.mat

%% 

% For each Dff Image, do basic registration:
        % Within day alignmnet first...
        % Across day next, given all unique ROIs...


% plot the registered days as XMSS images

% Assign registration offsets to ROI matrix

% Under some threshold, group ROI centroids ( maybe 20 cells to start...) 

% Plot Place-ness for these cells, along with labeled RGB XMASS tree image
% in large subplot






